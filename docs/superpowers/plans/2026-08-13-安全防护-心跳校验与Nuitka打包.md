# 安全防护（心跳校验 + Nuitka 打包）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在用户端加入后台心跳校验，把用户端打包从 PyInstaller 切换为 Nuitka，防止软件流出后被绕过卡密长期使用。

**Architecture:** 心跳复用现有 `CardAuthClient.validate()` 与服务器 `/api/client/validate`；新增纯逻辑 `HeartbeatPolicy`（失败计数 + 宽限期判定）和后台 `HeartbeatThread`（QThread），服务器拒绝即锁定，网络失败宽限 10 分钟。用户端改用 Nuitka 编译为原生单文件 exe，updater 与管理端继续用 PyInstaller。

**Tech Stack:** Python 3.12 / PySide6 / QThread / httpx / Nuitka / PyInstaller

## Global Constraints

- 本轮只改本地代码，不部署服务器；服务器代码 `server/app.py` 的 `/api/client/validate` 已存在且已测试，无需改动。
- 本仓库存在大量未跟踪的逆向资产与构建产物，**不做 git 提交**，以工作区文件为准。
- 管理端 `BydsjAdmin.exe` 与 `updater.exe` 保持 PyInstaller；仅用户端 `BydsjApp.exe` 切换 Nuitka。
- 心跳默认参数：校验间隔 30 分钟、服务器无响应宽限期 10 分钟、失败后每 60 秒重试一次。
- 所有测试统一用 `python -m pytest -q --basetemp=.pytest_tmp` 运行（系统 Temp 目录有权限问题）。
- 打包前必须关闭正在运行的 `BydsjApp.exe` / `BydsjAdmin.exe`，否则 exe 文件被占用。

---

## 文件结构

- Create: `scripts/heartbeat.py` —— 心跳宽限策略纯逻辑（可单测，不依赖 Qt）
- Create: `tests/test_heartbeat.py` —— 策略与线程级测试
- Modify: `app.py` —— 新增 `HeartbeatThread`、主窗口接线、`closeEvent` 停止线程、`_launch_updater` 兼容 Nuitka 的 updater 定位
- Modify: `packaging/build.py` —— 新增 Nuitka 用户端构建入口，保留 PyInstaller 分支
- Modify: `docs/更新日志.md`、`docs/安全防护-优化文档.md` —— 登记/勾选完成项

---

### Task 1: 心跳宽限策略纯逻辑

**Files:**
- Create: `scripts/heartbeat.py`
- Test: `tests/test_heartbeat.py`

**Interfaces:**
- Consumes: 无
- Produces:
  - `HeartbeatPolicy(interval_seconds: int = 1800, grace_seconds: int = 600)`
  - `policy.record_success() -> None`
  - `policy.record_failure(now: float | None = None) -> None`
  - `policy.consecutive_failures -> int`
  - `policy.interval_seconds -> int`
  - `policy.should_lock(now: float | None = None) -> bool`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_heartbeat.py
from scripts.heartbeat import HeartbeatPolicy


def test_first_failure_does_not_lock():
    policy = HeartbeatPolicy(interval_seconds=1800, grace_seconds=600)
    policy.record_failure(now=1000)
    assert policy.should_lock(now=1000) is False
    assert policy.should_lock(now=1599) is False


def test_lock_after_grace():
    policy = HeartbeatPolicy(interval_seconds=1800, grace_seconds=600)
    policy.record_failure(now=1000)
    assert policy.should_lock(now=1600) is True


def test_success_resets_failures():
    policy = HeartbeatPolicy(interval_seconds=1800, grace_seconds=600)
    policy.record_failure(now=1000)
    policy.record_success()
    assert policy.consecutive_failures == 0
    assert policy.should_lock(now=99999) is False


def test_consecutive_failures_count():
    policy = HeartbeatPolicy(interval_seconds=1800, grace_seconds=600)
    policy.record_failure(now=1)
    policy.record_failure(now=2)
    assert policy.consecutive_failures == 2


def test_interval_attribute():
    policy = HeartbeatPolicy(interval_seconds=1800, grace_seconds=600)
    assert policy.interval_seconds == 1800
```

- [ ] **Step 2: 运行确认失败**

Run: `.\.venv\Scripts\python.exe -m pytest tests\test_heartbeat.py -q --basetemp=.pytest_tmp`
Expected: FAIL，`ModuleNotFoundError: No module named 'scripts.heartbeat'`

- [ ] **Step 3: 实现最小代码**

```python
# scripts/heartbeat.py
"""心跳校验的宽限策略（纯逻辑，不依赖 Qt，便于单元测试）。"""

from __future__ import annotations

import time


class HeartbeatPolicy:
    def __init__(self, interval_seconds: int = 1800, grace_seconds: int = 600):
        self.interval_seconds = interval_seconds
        self.grace_seconds = grace_seconds
        self._consecutive_failures = 0
        self._first_failure_at: float | None = None

    @property
    def consecutive_failures(self) -> int:
        return self._consecutive_failures

    def record_success(self) -> None:
        self._consecutive_failures = 0
        self._first_failure_at = None

    def record_failure(self, now: float | None = None) -> None:
        now = now if now is not None else time.time()
        self._consecutive_failures += 1
        if self._first_failure_at is None:
            self._first_failure_at = now

    def should_lock(self, now: float | None = None) -> bool:
        if self._first_failure_at is None:
            return False
        now = now if now is not None else time.time()
        return now - self._first_failure_at >= self.grace_seconds
```

- [ ] **Step 4: 运行确认通过**

Run: `.\.venv\Scripts\python.exe -m pytest tests\test_heartbeat.py -q --basetemp=.pytest_tmp`
Expected: 5 passed

---

### Task 2: 用户端心跳线程与主窗口接线

**Files:**
- Modify: `app.py`
- Test: `tests/test_heartbeat.py`（追加线程级测试）

**Interfaces:**
- Consumes: `HeartbeatPolicy`（Task 1）、`CardAuthClient`、`CardAuthError`、`machine_id`（均已存在）
- Produces:
  - `HeartbeatThread(server_url: str, card_key: str, interval_seconds: int = 1800, grace_seconds: int = 600, retry_interval_seconds: int = 60, client: CardAuthClient | None = None)`
  - 信号：`ok = Signal()`、`rejected = Signal(str)`、`network_error = Signal(str)`、`locked = Signal(str)`
  - `thread.stop() -> None`
  - `MainWindow(store, card_key, server_url: str | None = None)`（构造函数新增可选参数）

- [ ] **Step 1: 写失败测试（线程级）**

```python
# tests/test_heartbeat.py 追加
import os

os.environ.setdefault("QT_QPA_PLATFORM", "offscreen")

import pytest
from PySide6.QtCore import QCoreApplication, QEventLoop, QTimer


class FakeAuthClient:
    def __init__(self, error: Exception | None = None):
        self.error = error
        self.calls = 0

    def validate(self, key: str, machine: str) -> dict:
        self.calls += 1
        if self.error is not None:
            raise self.error
        return {"ok": True}


@pytest.fixture(scope="module")
def qapp():
    app = QCoreApplication.instance() or QCoreApplication([])
    return app


def _run_thread(thread, signal_name: str, timeout_ms: int = 5000):
    from app import HeartbeatThread

    loop = QEventLoop()
    payload = {}
    getattr(thread, signal_name).connect(
        lambda *args: (payload.update(message=args[0] if args else ""), loop.quit())
    )
    thread.finished.connect(loop.quit)
    thread.start()
    QTimer.singleShot(timeout_ms, loop.quit)
    loop.exec()
    thread.wait(2000)
    return payload


def test_heartbeat_thread_rejects_invalid_card(qapp):
    from app import HeartbeatThread
    from scripts.card_auth import CardAuthError

    thread = HeartbeatThread(
        "http://example.test",
        "card",
        interval_seconds=0,
        grace_seconds=600,
        retry_interval_seconds=0,
        client=FakeAuthClient(error=CardAuthError("卡密已过期")),
    )
    payload = _run_thread(thread, "rejected")
    assert payload["message"] == "卡密已过期"
    assert thread._client.calls >= 1


def test_heartbeat_thread_locks_after_network_failure(qapp):
    from app import HeartbeatThread

    thread = HeartbeatThread(
        "http://example.test",
        "card",
        interval_seconds=0,
        grace_seconds=0,
        retry_interval_seconds=0,
        client=FakeAuthClient(error=RuntimeError("timeout")),
    )
    payload = _run_thread(thread, "locked")
    assert "timeout" in payload["message"]
    assert thread._client.calls >= 1
```

- [ ] **Step 2: 运行确认失败**

Run: `.\.venv\Scripts\python.exe -m pytest tests\test_heartbeat.py -q --basetemp=.pytest_tmp`
Expected: FAIL，`ImportError: cannot import name 'HeartbeatThread' from 'app'`

- [ ] **Step 3: 在 app.py 新增 HeartbeatThread**

在 `ActivationDialog` 之后（约 `app.py:353`）插入：

```python
class HeartbeatThread(QThread):
    ok = Signal()
    rejected = Signal(str)
    network_error = Signal(str)
    locked = Signal(str)

    def __init__(
        self,
        server_url: str,
        card_key: str,
        interval_seconds: int = 1800,
        grace_seconds: int = 600,
        retry_interval_seconds: int = 60,
        client: CardAuthClient | None = None,
    ):
        super().__init__()
        self._client = client or CardAuthClient(server_url)
        self._card_key = card_key
        self._machine = machine_id()
        self._policy = HeartbeatPolicy(interval_seconds, grace_seconds)
        self._retry_interval = retry_interval_seconds
        self._stop = False

    def stop(self) -> None:
        self._stop = True

    def run(self) -> None:
        while not self._stop:
            wait_seconds = (
                self._retry_interval
                if self._policy.consecutive_failures > 0
                else self._policy.interval_seconds
            )
            if not self._sleep(wait_seconds):
                break
            try:
                self._client.validate(self._card_key, self._machine)
                self._policy.record_success()
                self.ok.emit()
            except CardAuthError as exc:
                self.rejected.emit(str(exc))
                break
            except Exception as exc:  # noqa: BLE001
                self._policy.record_failure()
                if self._policy.should_lock():
                    self.locked.emit(str(exc))
                    break
                self.network_error.emit(str(exc))

    def _sleep(self, seconds: float) -> bool:
        waited = 0.0
        while waited < seconds and not self._stop:
            time.sleep(min(1.0, seconds - waited))
            waited += min(1.0, seconds - waited)
        return not self._stop
```

同步修改 `app.py` 顶部导入（约第 49-66 行区域）：

```python
from scripts.card_auth import CardAuthClient, CardAuthError, derive_key, machine_id, profile_dir
from scripts.heartbeat import HeartbeatPolicy
```

- [ ] **Step 4: 在 MainWindow 接线**

`MainWindow.__init__` 签名改为：

```python
def __init__(self, store: LocalAccountStore, card_key: str, server_url: str | None = None):
```

在 `__init__` 开头保存：

```python
self._server_url = server_url or DEFAULT_SERVER_URL
```

在 `__init__` 末尾（UI 全部构建完后）追加：

```python
self._heartbeat = HeartbeatThread(self._server_url, card_key)
self._heartbeat.ok.connect(self._on_heartbeat_ok)
self._heartbeat.rejected.connect(self._on_heartbeat_rejected)
self._heartbeat.network_error.connect(self._on_heartbeat_network_error)
self._heartbeat.locked.connect(self._on_heartbeat_locked)
self._heartbeat.start()
```

在 MainWindow 内新增处理方法与 `closeEvent`（放在 `_on_refresh` 附近）：

```python
def _on_heartbeat_ok(self):
    self._append_log("心跳校验通过")

def _on_heartbeat_network_error(self, message: str):
    self._append_log(f"心跳校验网络异常，宽限期内自动重试：{message}")

def _on_heartbeat_rejected(self, message: str):
    self._append_log(f"心跳校验失败，软件即将退出：{message}")
    QMessageBox.critical(self, "卡密已失效", f"{message}\n请联系管理员。")
    self.close()

def _on_heartbeat_locked(self, message: str):
    self._append_log(f"心跳长时间无法连接服务器，软件已锁定：{message}")
    QMessageBox.critical(self, "卡密已失效", "服务器长时间无法校验卡密，软件已锁定。\n请联系管理员。")
    self.close()

def closeEvent(self, event):
    self._heartbeat.stop()
    self._heartbeat.wait(3000)
    super().closeEvent(event)
```

`main()` 创建窗口处改为：

```python
win = MainWindow(dialog.store, dialog.card_key, dialog.server_url)
```

- [ ] **Step 5: 运行测试确认通过**

Run: `.\.venv\Scripts\python.exe -m pytest tests\test_heartbeat.py -q --basetemp=.pytest_tmp`
Expected: 7 passed

---

### Task 3: updater 定位兼容 Nuitka

**Files:**
- Modify: `app.py`（`_launch_updater`）

**Interfaces:**
- Consumes: 现有 `_launch_updater(target: str, new_path: str)`
- Produces: 无新接口；行为不变，但 updater 可执行文件的候选路径扩展为 PyInstaller/Nuitka 两种

- [ ] **Step 1: 改造 `_launch_updater` 的 updater 定位逻辑**

把 `_launch_updater` 中“查找 updater_src”的代码替换为：

```python
updater_src = None
if getattr(sys, "frozen", False):
    meipass = getattr(sys, "_MEIPASS", None)
    candidates = []
    if meipass:
        candidates.append(Path(meipass) / "updater" / "updater.exe")
    candidates.append(Path(sys.executable).resolve().parent / "updater" / "updater.exe")
    candidates.append(Path(sys.executable).resolve().parent / "updater.exe")
    for candidate in candidates:
        if candidate.exists():
            updater_src = candidate
            break
else:
    candidate = Path(__file__).resolve().parent.parent / "dist" / "updater.exe"
    if candidate.exists():
        updater_src = candidate
```

其余复制、启动、退出逻辑不变。

- [ ] **Step 2: 语法与回归验证**

Run: `.\.venv\Scripts\python.exe -m py_compile app.py`
Run: `.\.venv\Scripts\python.exe -m pytest -q --basetemp=.pytest_tmp`
Expected: 编译通过；全部测试通过（当前基线 105 个 + Task 2 新增 2 个 = 107 个）

---

### Task 4: packaging/build.py 增加 Nuitka 用户端构建

**Files:**
- Modify: `packaging/build.py`

**Interfaces:**
- Consumes: 现有 `run_pyinstaller`、`ROOT`、`DIST`
- Produces:
  - `run_nuitka(args: list[str]) -> None`
  - `build_nuitka_app() -> None`（生成 `dist/BydsjApp.exe`）
  - `main()` 支持命令行参数 `--user-app nuitka|pyinstaller`，默认 `nuitka`

- [ ] **Step 1: 先安装 Nuitka 工具链并验证（一次性）**

Run: `.\.venv\Scripts\python.exe -m pip install nuitka`
Run: `.\.venv\Scripts\python.exe -m nuitka --version`

本机未检测到 `cl`/`gcc`，因此构建命令固定使用 `--mingw64 --assume-yes-for-downloads`，让 Nuitka 自动下载 MinGW64。此步骤需要联网，耗时较长。

- [ ] **Step 2: 修改 packaging/build.py**

在文件顶部追加 `import shutil`；新增：

```python
def run_nuitka(args: list[str]) -> None:
    cmd = [
        sys.executable,
        "-m",
        "nuitka",
        "--onefile",
        "--standalone",
        "--windows-console-mode=disable",
        "--enable-plugin=pyside6",
        "--assume-yes-for-downloads",
        "--mingw64",
        *args,
    ]
    print("RUN:", " ".join(cmd))
    subprocess.run(cmd, cwd=str(ROOT), check=True)


def build_nuitka_app() -> None:
    DIST.mkdir(exist_ok=True)
    run_nuitka(
        [
            "--output-dir",
            str(DIST / "nuitka_build"),
            "--output-filename=BydsjApp.exe",
            "--include-data-file",
            f"{ROOT / 'data' / 'flow5_template.bin'}=data/flow5_template.bin",
            "--include-data-file",
            f"{ROOT / 'data' / 'tokenlogin_template.bin'}=data/tokenlogin_template.bin",
            "--include-data-dir",
            f"{ROOT / 'assets'}=assets",
            "--include-data-binary",
            f"{DIST / 'updater.exe'}=updater/updater.exe",
            "--nofollow-import-to",
            "mitmproxy,frida,pytest,paramiko",
            str(ROOT / "app.py"),
        ]
    )
    built = next(DIST.glob("nuitka_build/**/BydsjApp.exe"), None)
    if built is None:
        raise SystemExit("Nuitka 未生成 BydsjApp.exe")
    shutil.copy2(built, DIST / "BydsjApp.exe")
    print("Nuitka 用户端完成：", DIST / "BydsjApp.exe")
```

`main()` 改为：

```python
def main() -> None:
    user_app = "nuitka"
    if len(sys.argv) > 1 and sys.argv[1] == "--user-app":
        if len(sys.argv) < 3 or sys.argv[2] not in ("nuitka", "pyinstaller"):
            raise SystemExit("用法：python packaging/build.py [--user-app nuitka|pyinstaller]")
        user_app = sys.argv[2]

    DIST.mkdir(exist_ok=True)

    # 1) updater（用户端内嵌，保持 PyInstaller）
    run_pyinstaller(
        [
            "--onefile",
            "--noconsole",
            "--name",
            "updater",
            str(ROOT / "updater.py"),
        ]
    )

    # 2) 用户端（默认 Nuitka，可回退 PyInstaller）
    if user_app == "nuitka":
        build_nuitka_app()
    else:
        run_pyinstaller(
            [
                "--onefile",
                "--windowed",
                "--name",
                "BydsjApp",
                "--icon",
                str(ROOT / "assets" / "app_icon.ico"),
                "--add-data",
                f"{ROOT / 'data' / 'flow5_template.bin'};data",
                "--add-data",
                f"{ROOT / 'data' / 'tokenlogin_template.bin'};data",
                "--add-data",
                f"{ROOT / 'assets' / 'items'};assets/items",
                "--add-data",
                f"{ROOT / 'assets' / 'app_icon.ico'};assets",
                "--add-binary",
                f"{DIST / 'updater.exe'};updater",
                "--exclude-module",
                "mitmproxy",
                "--exclude-module",
                "frida",
                "--exclude-module",
                "pytest",
                "--exclude-module",
                "paramiko",
                str(ROOT / "app.py"),
            ]
        )

    # 3) 管理端（保持 PyInstaller）
    run_pyinstaller(
        [
            "--onefile",
            "--windowed",
            "--name",
            "BydsjAdmin",
            str(ROOT / "admin_app.py"),
        ]
    )

    print("打包完成：", DIST)
```

- [ ] **Step 3: 语法验证**

Run: `.\.venv\Scripts\python.exe -m py_compile packaging\build.py`
Expected: 编译通过

- [ ] **Step 4: 真机打包验证（耗时较长，约 15-40 分钟）**

Run: `.\.venv\Scripts\python.exe packaging\build.py --user-app nuitka`
Expected: `dist/BydsjApp.exe` 存在，且 `dist/updater.exe`、`dist/BydsjAdmin.exe` 均存在

- [ ] **Step 5: 打包产物冒烟验证（人工）**

1. 启动 `dist\BydsjApp.exe`，确认窗口正常、图标正常、道具图标正常显示；
2. 触发一次“检查更新”并下载新版本，确认 `updater.exe` 能解压并替换 exe（热更新链路兼容 Nuitka）；
3. 确认日志区/本地日志目录仍然可写。

---

### Task 5: 文档登记与全量回归

**Files:**
- Modify: `docs/更新日志.md`
- Modify: `docs/安全防护-优化文档.md`

**Interfaces:**
- Consumes: Task 1-4 的完成结果
- Produces: 更新后的文档清单

- [ ] **Step 1: 更新 `docs/更新日志.md` 的 1.0.1 区块**

追加两条已登记项：

```markdown
  - [x] 用户端：新增后台心跳校验（每 30 分钟校验卡密，网络失败宽限 10 分钟，服务器拒绝或超时即锁定退出）
  - [x] 用户端：打包切换 Nuitka 原生编译（updater 与管理端保持 PyInstaller，热更新链路兼容）
```

- [ ] **Step 2: 更新 `docs/安全防护-优化文档.md`**

将“实施清单”勾选：

```markdown
- [x] 用户端新增心跳校验线程与锁定逻辑
- [x] 用户端打包切换 Nuitka（更新 packaging 脚本）
- [x] 验证：心跳锁定、服务器断线宽限、热更新兼容、exe 可运行
- [x] 全量 pytest 回归
- [ ] （可选）管理端登录限流、HTTPS
- [ ] 更新项目记忆与打包清单文档
```

并把文档状态改为“已实施（随 1.0.1 发布）”。

- [ ] **Step 3: 全量回归**

Run: `.\.venv\Scripts\python.exe -m pytest -q --basetemp=.pytest_tmp`
Expected: 107 passed（或与最终代码一致的通过数，不允许失败）

---

## Self-Review 记录

- 覆盖检查：心跳策略（Task 1）、心跳线程与锁定弹窗（Task 2）、Nuitka 打包脚本（Task 4）、热更新兼容（Task 3 + Task 4 Step 5）、文档登记（Task 5）；服务器端无需改动（validate 已存在）。
- 占位符检查：无 TBD/TODO；每个代码步骤都有可运行的实际代码。
- 类型一致性：`HeartbeatPolicy` 的构造与属性在 Task 1/2 中一致；`HeartbeatThread` 的 `client` 注入与测试中的 `FakeAuthClient.validate(key, machine)` 签名一致；`MainWindow(store, card_key, server_url)` 在 Task 2 与 `main()` 调用一致。
